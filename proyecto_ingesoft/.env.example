# ====================================
# CONFIGURACIÓN DE VARIABLES DE ENTORNO
# TurismoApp Backend
# ====================================

# Configuración de la Base de Datos PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=turismo_db
DB_USER=postgres
DB_PASSWORD=tu_password_postgresql

# Configuración de Autenticación JWT
JWT_SECRET=tu_clave_secreta_muy_segura_aqui_cambiala_en_produccion
JWT_EXPIRES_IN=7d

# Configuración del Servidor
PORT=4000
NODE_ENV=development

# CORS (Configuración de seguridad)
CORS_ORIGIN=http://localhost:3000

# ====================================
# INSTRUCCIONES:
# ====================================
# 1. Copia este archivo a /backend/.env
# 2. Cambia 'tu_password_postgresql' por tu contraseña real de PostgreSQL
# 3. Cambia 'tu_clave_secreta_muy_segura_aqui' por una clave segura aleatoria
# 4. En producción, usa variables de entorno reales y no archivos .env
