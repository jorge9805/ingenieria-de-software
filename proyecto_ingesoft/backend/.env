# ====================================
# CONFIGURACIÓN DE VARIABLES DE ENTORNO
# TurismoApp Backend
# ====================================

# Configuración de la Base de Datos SQLite
# (La base de datos se crea automáticamente en /backend/database/turismo.db)
DB_TYPE=sqlite

# Configuración de Autenticación JWT
JWT_SECRET=tu_clave_secreta_muy_segura_aqui_cambiala_en_produccion
JWT_EXPIRES_IN=7d

# Configuración del Servidor
PORT=4000
NODE_ENV=development

# CORS (Configuración de seguridad)
CORS_ORIGIN=http://localhost:5173

# ====================================
# INSTRUCCIONES:
# ====================================
# 1. Copia este archivo a /backend/.env
# 2. Cambia 'tu_clave_secreta_muy_segura_aqui' por una clave segura aleatoria
# 3. ¡La base de datos SQLite se configura automáticamente! No necesitas PostgreSQL
# 4. En producción, usa variables de entorno reales y no archivos .env
